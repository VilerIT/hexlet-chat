//- Profile Detail component.
//-
//- Represents a profile detail. Must be placed in an <ul> list.
//-
//- title - the title of the detail.
//- value - the value of the detail.
//- iconSrc - the icon source that will be displayed next to the title and value.

mixin profileDetail(title, value, iconSrc)
  li.list-group-item
    .d-flex.justify-content-between.align-items-center
      div
        span.small.text-muted= title
        p.mb-0.small= value
      svg.w-15px.h-15px.text-black-50(fill='currentColor')
        use(xlink:href=`${iconSrc}`)
